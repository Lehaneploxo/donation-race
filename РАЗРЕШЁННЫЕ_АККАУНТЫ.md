# Аккаунты, с которых можно запускать наши игры

Список синхронизирован с `ALLOWED_STREAMERS` в `server/server.js` (только эти TikTok-ники могут открыть комнату и запустить игру).

- lehaneploxo
- utilizator11123
- tiktokgame8805
- demo

Чтобы добавить или убрать аккаунт — правь список тут и в `server/server.js` (`ALLOWED_STREAMERS`), затем деплой на Railway.
